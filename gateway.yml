spring:
  profiles:
    active: development

---
spring:
  profiles: development
  rabbitmq:
    host: 172.31.25.252
    port: 5672
    username: guest
    password: guest
   
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.31.22.222:8761/eureka/
    
zuul:
  routes:
    user-service:
      path: /api/user/**
    todo-service:
      path: /api/todo/**

---
spring:
  profiles: vm
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.31.22.222:8761/eureka/
      
zuul:
  routes:
    user-service:
      path: /api/user/**
    todo-service:
      path: /api/todo/**
