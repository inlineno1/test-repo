spring:
  profiles:
    active: development

---
spring:
  profiles: development
   
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.31.22.222:8761/eureka/
    
zuul:
  routes:
    user-service:
      path: /api/user/**
    todo-service:
      path: /api/todo/**

---
spring:
  profiles: vm
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.31.22.222:8761/eureka/
      
zuul:
  routes:
    user-service:
      path: /api/user/**
    todo-service:
      path: /api/todo/**
